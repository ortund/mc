using MedCore;
using MedCore.Claim;
using NUnit.Framework;
using System;

namespace Tests
{
    [TestFixture]
    public class GPTest
    {
        private GpClaim gpClaim;

        [SetUp]
        public void SetUp()
        {
        }
    }
}
